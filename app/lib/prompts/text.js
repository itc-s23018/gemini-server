const prompt = `
以下は会話の履歴と保存済みの専門用語です。
最後のメッセージに対して、自然で適切な返答を3つ提案してください。
可能であれば保存済み用語を活用してください。
出力は必ず以下の形式で返してください：

返答1: ○○○
返答2: ○○○
返答3: ○○○

会話履歴:
${historyText}

保存済み用語:
${wordsText}

最後のメッセージ: 「${latestMessage}」

返答の提案:
`.trim();
